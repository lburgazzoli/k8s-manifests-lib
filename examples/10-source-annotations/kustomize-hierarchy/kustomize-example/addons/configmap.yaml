apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  environment: production
  log_level: info
  feature_flags: |
    {
      "enable_metrics": true,
      "enable_tracing": true,
      "cache_ttl": 300
    }
